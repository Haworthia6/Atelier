var controller = require('../controllers');
var router = require('express').Router();

// Connect controller methods to corresponding routes
// router.method('/route', controller.route.method)
module.exports = router;