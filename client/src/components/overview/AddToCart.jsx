import React, {useState} from 'react';
import { useSelector, useDispatch } from 'react-redux'

function AddToCart (props) {
  const dis = useDispatch();

  return (
    <div>This is the add to cart section
    </div>
  )
}

export default AddToCart;